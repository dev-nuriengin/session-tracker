# CLAUDE.md

`GIT_SCOPE: personal` <!-- personal project → git account dev-nuriengin -->

**Read [`AGENTS.md`](./AGENTS.md) first** — the agent onboarding doc (what this project
is, the mental model, how to work here). Then [`README.md`](./README.md) for the full
design and [`_tracker.md`](./_tracker.md) for current build status (temporary scaffold).

Quick reminders:
- The tracker is a **brain/memory** — it does NOT do work or gate coding.
- **MCP is the heart**; core store is **local Postgres**; all data is **local & private**.
- **Plan-before-code is our build workflow, not a tracker feature.**
- Tick `_tracker.md` at session end. **Never commit tracker DATA — only app code.**
- **Never commit/push without an explicit "yes"** (account `dev-nuriengin`).
